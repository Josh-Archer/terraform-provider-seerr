---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "seerr_radarr_quality_profile Data Source - seerr"
subcategory: ""
description: |-
  Look up a Radarr quality profile by name via /api/v3/qualityprofile.
---

# seerr_radarr_quality_profile (Data Source)

Look up a Radarr quality profile by name via /api/v3/qualityprofile.

## Example Usage

```terraform
data "seerr_radarr_quality_profile" "movies" {
  url     = var.radarr_url
  api_key = var.radarr_api_key
  name    = "HD-1080p"
}

resource "seerr_radarr_server" "movies" {
  url                = var.radarr_url
  api_key            = var.radarr_api_key
  quality_profile_id = data.seerr_radarr_quality_profile.movies.quality_profile_id
  active_directory   = "/media/movies"
}
```

## Schema

### Required

- `api_key` (String, Sensitive)
- `name` (String)

### Optional

- `base_url` (String)
- `hostname` (String)
- `port` (Number)
- `url` (String)
- `use_ssl` (Boolean)

### Read-Only

- `quality_profile_id` (Number)
- `response_json` (String)
- `status_code` (Number)
